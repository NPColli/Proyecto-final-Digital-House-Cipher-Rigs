<!DOCTYPE html>
<html>

<head>
  <title>Rigs</title>
  <%- include('../partials/head') %>
</head>

<body>
  <%- include ('../partials/header') %>

  <main>
    <video class="video" preload="auto" autoplay loop muted>
      <source src="img/home.mp4" type="video/mp4">
      <source src="img/home.mp4" btnvertype="video/webm">
      <source src="img/home.mp4" type="video/ogg">
  </video>

  <h1 class="h1form">RIGS para mineria | ETH/ETC </h1>
  <div class="adminview">
    <% if (locals.userLogged && locals.userLogged.rol===0) { %>
      <button class="buttons"><a class="buttonCrear" href="./rigs/crear">Agregar RIG</a></button>
             <% } %>
            </div>
          <br><br>
  <section class="product"> 
    <button class="pre-btn"><img src="img/flecha.svg" alt=""></button>
    <button class="nxt-btn"><img src="img/flecha.svg" alt=""></button>
    <div class="product-container">
      <% for (let i = 0; i < rigs.length; i++) { %>
        <div class="product-card">
            <div class="product-image2">
              <img src="../img/rigs/<%=rigs[i].image %>" class="product-thumb" alt="La imagen no pudo cargar correctamente">
                <button class="card-btn"><a href="/rigs/<%=rigs[i].id_rigs%>">Ver más</button></a>
            </div>
            <% if (locals.userLogged && locals.userLogged.rol===0) { %>
              <button class="button"><a href="/rigs/editar/<%=rigs[i].id_rigs%>">Editar RIG</button></a>
              <% } %>
            <div class="product-info">
                <h2 class="product-brand"><%=rigs[i].titulo %></h2>
                <p class="product-short-description"><%=rigs[i].especificaciones %></p>
                <span class="price2"><%=rigs[i].precio %></span>
            </div>
        </div>
        <% } %>
    </div>
      </section>
  </main>
  <footer>
    <%- include('../partials/footer') %>
  </footer>
  <script>
    const productContainers = [...document.querySelectorAll('.product-container')];
const nxtBtn = [...document.querySelectorAll('.nxt-btn')];
const preBtn = [...document.querySelectorAll('.pre-btn')];

productContainers.forEach((item, i) => {
    let containerDimensions = item.getBoundingClientRect();
    let containerWidth = containerDimensions.width;

    nxtBtn[i].addEventListener('click', () => {
        item.scrollLeft += containerWidth;
    })

    preBtn[i].addEventListener('click', () => {
        item.scrollLeft -= containerWidth;
    })
})
  </script>
</body>

</html>